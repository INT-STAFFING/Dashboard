# Vercel Postgres connection string (optional in dev — falls back to in-memory seed data)
POSTGRES_URL=postgres://user:password@host:5432/dbname

# Secret required to access the /upload page and POST /api/upload
UPLOAD_SECRET=cambia_questo_in_produzione
